import { scoreCalculator } from 'js-team-week/helpers/score-calculator';
import { module, test } from 'qunit';

module('Unit | Helper | score calculator');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = scoreCalculator([42]);
  assert.ok(result);
});
